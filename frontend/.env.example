# Vite env: prefix public variables with VITE_
# Base URL for your API (no trailing slash)
VITE_API_URL=http://localhost:3000

# Stripe publishable key (only publishable key in frontend)
VITE_STRIPE_PK=pk_live_...
